spring:
  application:
    name: ContactHub

  datasource:
    url: jdbc:postgresql://localhost:5432/contacthubDB
    username: postgres
    password: noemak

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true